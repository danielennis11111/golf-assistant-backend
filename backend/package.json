{
  "name": "golf-assistant-backend",
  "version": "1.0.0",
  "description": "Backend server for Golf Assistant Pro",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production node dist/index.js",
    "build": "rm -rf dist && tsc",
    "dev": "NODE_ENV=development ts-node-dev --respawn --transpile-only src/index.ts",
    "postinstall": "npm run build",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@google-cloud/vision": "^5.1.0",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.11.24",
    "@types/sharp": "^0.31.1",
    "canvas": "^3.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.2",
    "sharp": "^0.34.1",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  }
}
